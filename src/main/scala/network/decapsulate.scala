package decapsulate

/*
  Transform the messages receive from the network into abstract messages
  which can be used to interact with the game
*/

import messages._
import model._

class MessageReader() {
  // read message
}
